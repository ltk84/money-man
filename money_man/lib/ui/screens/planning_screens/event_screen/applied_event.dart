import 'package:flutter/material.dart';

class AppliedEvent extends StatelessWidget {
  const AppliedEvent({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.only(top: 30),
      color: Color(0xff1a1a1a),
      //child: MyBudgetTile(),
      child: Column(
      ),
    );
  }
}
